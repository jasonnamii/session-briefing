---
name: session-briefing
description: "세션 브리핑 자동화. 작업 완료시 결정사항·미결·다음액션을 Agent-Ops/_session-briefs/에 자동 저장. 세션 종료·작업 완료·15턴↑·'이어서' 시 자동발동. 세션브리핑, session briefing, 이어서, 작업 완료, 세션 저장 맥락에서 사용. NOT: UP수정(→up-manager), 산출물저장(→deliverable-engine)."
---

<!-- Trigger Conditions
P1: 세션브리핑, 세션저장, session briefing, 이어서, 작업완료.
P2: 브리핑해줘, 저장해줘, 이어서, resume, brief, save session.
P3: session brief, context preservation, session continuity.
P4: 작업 완료 후 자동, 15턴 이상 도달시, 새 세션 시작시 이전 브리핑 로드.
P5: .md로, _session-briefs/에.
NOT: UP수정(→up-manager), 산출물저장(→deliverable-engine), 일반문서작성(→직접수행).
-->

# Session Briefing

세션 간 컨텍스트 연속성을 보장하는 스킬. 작업 결과를 고정 경로에 저장하고, 다음 세션에서 자동 로드.

---

## 발동 조건

| 조건 | 행동 |
|------|------|
| 작업 완료 | 자동실행 (제안 아님) |
| 15턴↑ 도달 | 자동실행 + 1줄 보고 |
| 맥락 임계 (주요작업 3건↑) | 자동실행 + 1줄 보고 |
| 형 "이어서" | 최신 브리핑 자동 로드 + 작업 재개 |
| 새 세션 시작 | PATH에서 최신 브리핑 1건 자동 로드 |
| 형 "생략" | 1회 스킵 |

---

## 경로 · 네이밍

```
PATH: Agent-Ops/_session-briefs/YYMMDD_[태그]_작업명.md
```

### TIME_ACQUIRE
env date의 YYMMDD 사용. HHmm **파일명에서 제거**. 같은 날 중복 시 접미 `_2` 추가.

이유: bash date 호출은 샌드박스 왕복 2-5초. 날짜 단위 식별로 충분하고 중복은 접미사로 해결.

### 네이밍 규칙
- 태그: 프로젝트 폴더명 축약 (예: "C8BP")
- 프로젝트 미확정 → 태그 생략

---

## 콘텐츠

3블록 (순서 고정):

1. **결정사항** — 이번 세션에서 확정된 것
2. **미결** — 보류 중인 것 + 사유 + 외부 대기 항목(의존성 포함)
3. **다음액션** — 다음 세션 할 일 + 관련 파일 경로(컨텍스트 포인터 포함)

---

## 문서화 전환 게이트

산출물 작성 완료 후 → 브리핑 전에 문서화 전환 제안 가능:

| 조건 | 행동 |
|------|------|
| 합의 3조건 ALL (❶핵심방향 명시적 동의 ❷수정요청 없음 ❸재검토 신호 없음) | "문서로 옮길까요?" 제안 |
| "써줘/저장해" | 즉시 실행 |
| spine 미합의 | 문서화 ✗ |

---

## 브리핑 없이 종료 = 컨텍스트 소실

이유: Claude의 세션 간 메모리는 세션 브리핑이 유일한 장치. 저장하지 않으면 다음 세션에서 처음부터 시작해야 한다.

---

## 속도 규칙

| 규칙 | 이유 |
|------|------|
| bash date 호출 금지 — env date 사용 | 샌드박스 왕복 제거 (-3초) |
| 디렉토리 존재 확인 생략 — 쓰기 실패 시에만 create | MCP 호출 1회 제거 (-2초) |
| FS write_file 단일 호출로 완료 | 최소 도구 호출 |

---

## Gotchas

- **프로젝트 태그 과잉:** "C8-Business-Plan-Meeting-2026-04" 대신 "C8BP" 수준으로 축약
- **브리핑 과다:** 일반 대화에서 매번 저장 → 노이즈. 발동 조건표 엄격 준수
- **이어서 미로드:** "이어서"인데 브리핑 로드 없이 새로 시작 → 형 중복 설명 필요
- **같은 날 중복:** 파일명에 HHmm 없으므로 기존 파일 확인 후 `_2` 접미 추가
- (실수 발견 시 직접 추가)
